name: Build am4 wheel

on:
  workflow_dispatch:
  # Tag releases use `.github/workflows/release.yml` (builds wheel + installer once).

jobs:
  build-am4-wheel:
    uses: ./.github/workflows/_build-am4-wheel-reusable.yml

